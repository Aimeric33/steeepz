<main class="app__container">
    <h1>My workspaces</h1>

    <% @workspaces.each do |workspace| %>
      <h2><%= workspace.name %></h2>
      <%= link_to 'Open', workspace_path(workspace), class: 'button--primary' %>
      <div>---</div>
    <% end %>
</main>
