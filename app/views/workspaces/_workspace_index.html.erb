<% workspaces.each do |workspace| %>
  <%= link_to workspace_path(workspace), class: "ws__block" do %>
    <div class="ws__content">
      <h2 class="ws__name"><%= workspace.name %></h2>
      <div class="ws__details">Last update on <%= workspace.updated_at.strftime("%d/%m/%Y") %>&nbsp; · &nbsp;<%= workspace.checklists.count %> checklists</div>
    </div>
  <% end %>
<% end %>
