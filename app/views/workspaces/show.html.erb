<%= link_to 'Back to my workspaces', workspaces_path %>

<h1><%= @workspace.name %></h1>
<% @workspace.checklists.each do |checklist| %>
  <h2><%= checklist.name %></h2>
  <%= link_to 'Open', checklist_path(checklist) %>
  <div>---</div>
<% end %>
